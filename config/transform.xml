<!-- format transformation information used by transformation service -->
<!-- 
<transformation ID='xxx'>: define the transformation identifier

	<instruction>: define the transformation instruction on the transformation identifier 'xxx'.  It
	is the command line that will be used to carry out the format transformation.  
	for example, "ffmpeg -i #INPUT_FILE# -sameq -acodec pcm_s16le -y #OUTPUT_FILE#" means the ffmpeg 
	will be used to perform the actual format transformation associated with the proper commmand line argument.  
	It is assumed that the specified tool, ffmpeg, is installed properly on the desktop where 
	the transformation service is running.  INPUT_FILE is used by the transformation to specify the
	input of the transformation instruction and OUTPUT_FILE is used to store the output of the format
	transformation.
	
	<extension> : define the file extension that will be used to create the output file.  This is
	to ensure the format for output of the transformation can be properly identified and validated.
	
	<identifier> : the agent identifier for the transformation service, to be used to generate proper
	versioned premis agent.
	
	<software> : describe the software that is used for the format transformation.  The software description
	is then used to generate premis agent detail.
-->

<transformations>
	<transformation ID='WAVE_NORM'>
		<instruction>ffmpeg -i #INPUT_FILE# -sameq -acodec pcm_s16le -y #OUTPUT_FILE#</instruction>
		<extension>.wav</extension>
		<identifier>wave/norm/v0.1.1</identifier>
		<software>
			FFmpeg version %{evr}, Copyright (c) 2000-2009 Fabrice Bellard, et al.
			  configuration: --prefix=/usr --libdir=/usr/lib64 --shlibdir=/usr/lib64 --mandir=/usr/share/man --enable-shared --enable-gpl --enable-nonfree --enable-pthreads --disable-avisynth --disable-vhook --disable-mmx --enable-libdc1394 --enable-libfaac --enable-libfaad --enable-libfaadbin --enable-libmp3lame --enable-libopenjpeg --enable-libspeex --enable-libtheora --enable-libvorbis --enable-libx264 --enable-libxvid --extra-cflags=-O2 -g -m64 -mtune=generic -fPIC --disable-stripping
			  libavutil     49.15. 0 / 49.15. 0
			  libavcodec    52.20. 1 / 52.20. 1
			  libavformat   52.31. 0 / 52.31. 0
			  libavdevice   52. 1. 0 / 52. 1. 0
			  built on May 10 2010 12:38:53, gcc: 4.1.2 20080704 (Red Hat 4.1.2-46)
		</software>
	</transformation>
	<transformation ID='AVI_NORM'>
		<instruction>mencoder #INPUT_FILE# -oac pcm -ovc lavc -lavcopts vcodec=mjpeg -o #OUTPUT_FILE#</instruction>
		<extension>.avi</extension>
		<identifier>avi/norm/v0.1.1</identifier>		
		<software>
			MEncoder SVN-r28728-snapshot-4.1.2 (C) 2000-2009 MPlayer Team
		</software>		
	</transformation>
	<transformation ID='MOV_NORM'>
		<instruction>lqt_transcode -ac rawaudio -vc mjpa #INPUT_FILE# #OUTPUT_FILE#</instruction>
		<extension>.mov</extension>
		<identifier>mov/norm/v0.1.1</identifier>
		<software>
		name                    libquicktime
		version              	1.1.5
		homepage                http://libquicktime.sourceforge.net/
		depends_lib             port:gettext port:jpeg port:libiconv port:libpng port:zlib port:ffmpeg port:faac port:faad2 port:libvorbis port:libogg port:lame
		configure.args          --without-gtk --without-alsa \
		                        --with-libjpeg --with-libpng --with-ffmpeg \
		                        --with-vorbis=${prefix} \
		                        --with-libiconv-prefix=${prefix} \
		                        --with-libintl-prefix=${prefix} \
		                        --enable-gpl
		</software>
	</transformation>
</transformations>